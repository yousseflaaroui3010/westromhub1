# Context Log
<!-- Track: ONGOING / DONE / LEFT / BLOCKED -->

| Date | Task | Status | Owner | Notes |
|------|------|--------|-------|-------|
| 2026-04-15 | Full project review + scoring | DONE | Claude | Score 5.5/10. Plan written to docs/REVIEW_PLAN.txt. 6 sprints defined. |
| 2026-04-15 | Sprint 1 — Critical bug fixes | DONE | Claude | BUG-1/2/3/4 + DRY-3 + SEC-4 all resolved. tsc clean, 6/6 tests pass. |
| 2026-04-15 | Sprint 2 — Security hardening | DONE | Claude | SEC-1/2/3/5/6 complete. Zod on all endpoints, CORS restricted, nginx headers, non-root Docker, prompt injection delimiters. |
| 2026-04-15 | Sprint 3 — pnpm + Hono + ESLint | DONE | Claude | pnpm-workspace.yaml + .npmrc added. Express→Hono migration (api/src/index.ts). ESLint flat configs (root + api). `no-explicit-any` fixed in OllamaStatus. `set-state-in-effect` downgraded to warn (async fetch-on-mount is correct pattern). |
| 2026-04-15 | Sprint 4 — AI fallback + DRY | DONE | Claude | Provider chain: Ollama→Groq (text), Ollama→OpenRouter (vision). `useDocumentUpload` hook extracted. `@` alias fixed to `./src`. DRY-2 (StatusCard) deferred — status enums differ per domain, abstraction adds complexity without clear gain. |
| 2026-04-15 | Sprint 5 — Docker healthchecks + API tests | DONE | Claude | DOCKER-1/2/3 complete (version removed, readiness loop, healthchecks on all 3 services). TEST-1/2/3/4/5 complete (vitest in api, 22 endpoint tests, 17 ruleEngine tests incl. 11 edge cases, CI api job, coverage thresholds). |
| 2026-04-15 | Sprint 6 — Prompts DRY + OpenAPI + E2E | DONE (partial) | Claude | DRY-6: prompts extracted to api/src/prompts.ts. OpenAPI JSON spec at /api/openapi.json + Swagger UI at /api/docs (CDN, no extra dep). SSE streaming + E2E (Playwright) deferred — no client-side streaming code wired. |
| 2026-04-17 | E001 — Favicon & Brand Identity | DONE | GitHub Copilot | US-001 (assets generated), US-002 (index.html updated), US-003 (manifest.json). |
| 2026-04-17 | E002 — SEO Foundation | DONE | GitHub Copilot | US-004 (canonical, og), US-005 (per-route titles/meta), US-006 (JSON-LD), US-007 (sitemap, robots.txt). |
| 2026-04-17 | E003 — Accessibility | DONE | GitHub Copilot | US-008 (skip nav), US-009 (aria mobile menu), US-010 (landmarks), US-011 (aria-describedby), US-012 (focus trap), US-013 (focus ring, noscript). |
| 2026-04-17 | Sprint review + fixes (senior pass) | DONE | Claude | 13 intern defects fixed (commit e8533ce) + 4 Opus-flagged (commit 5982f20): skip link `-top-9999` → `sr-only focus:not-sr-only`; removed `focus:outline-none` on `<main>`; `useScrollIntoViewOnFocus` made generic (dropped `as any`); extracted `getDeviceCapabilities` utility (DRY across Tax/Insurance); renamed from hook form (no hooks inside — rules-of-hooks); iOS heuristic tightened (`maxTouchPoints>1 && Macintosh UA`); redundant `motion-reduce:animate-none` removed (global CSS covers it); `motion-reduce:` added to 3 transform hovers (ResourceCard, PublicHeader logo, HomeView decorative divs); `setTimeout(50)` → `requestAnimationFrame` for focus mgmt; manifest.json `"any maskable"` split to 3 entries; og:title/twitter:title corrected; `.gitattributes` added + LF renormalized. |
| 2026-04-17 | US-010 role="main" added | DONE | Claude | Explicit `role="main"` attribute added to `<main>` in App.tsx to satisfy AC (redundant with HTML5 but AC required). |
| 2026-04-17 | US-017 font-display verified | DONE | Claude | All 7 imported @fontsource weights (inter 400/500/600, manrope 400/600/700/800) ship `font-display: swap` — confirmed via node_modules inspection. |
| 2026-04-17 | US-021 bundle audit logged | DONE | Claude | Prod build (vite 6): main `index.js` 260.93 kB (81.63 kB gz); lazy `pdf` 365.12 kB (107.61 kB gz); lazy `TaxView` 30.80 kB (8.47 kB gz); lazy `InsuranceView` 17.56 kB (5.12 kB gz); shared analysis chunk 31.34 kB (12.12 kB gz); `index.css` 83.47 kB (24.66 kB gz); pdf.worker 1.38 MB (on-demand only). Home initial payload: ~82 kB gz JS + ~25 kB gz CSS. |
| 2026-04-17 | src/main.tsx formatting cleanup | DONE | Claude | Intern had glued import statements onto single lines. Reformatted for readability (no behavior change). |
| 2026-04-17 | Mobile UX pass (as-a-user review) | DONE | Claude | 3 real mobile UX defects found + fixed: (1) currency inputs (currentValue/priorValue/zillowValue/realtorValue) were `type="text"` showing text keyboard on mobile — added `inputMode="numeric"` + `pattern="[0-9]*"` so iOS/Android surface the number pad; (2) error-state Retry button was ~36px tall (below 44px recommended thumb target) — bumped to `min-h-[44px] py-2.5`; (3) on mobile the results panel renders below the form with no auto-scroll, so users missed it — extracted `useScrollToResult` hook that smooth-scrolls result panel into view on first appearance (no-op on desktop 2-col layout). |
| 2026-04-17 | US-027 Lighthouse baseline captured + post-fix | DONE | Claude+PM | Baseline with extensions: Home 83/100/100/100, Taxes 70/97/96/100, Insurance 74/96/100/100. Extensions (Bitwarden) inflated TBT by 789ms — rerun in incognito for true numbers. Fixed: CLS 0.139→~0 (footer logo w-[110px]); text-gray-400→text-gray-500 (WCAG contrast); aria-label mismatch fixed. Report in docs/lighthouse-mobile-final.md. Performance gap is render-blocking font CSS — addressable via US-018 font preload (stretch goal). |
