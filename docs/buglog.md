# Bug Log
<!-- Phase 5+ — Bugs and root-cause fixes -->

| Date | Bug ID | Description | Root Cause | Fix Applied | Affected Files | Verified By |
|------|--------|-------------|------------|-------------|----------------|-------------|
| 2026-04-15 | BUG-1 | `OLLAMA_BASE_URL` rendered as "undefined" in offline modal | Variable never defined in component; imported from server env by mistake | Replaced with `API_BASE_URL` (already defined on line 4) | `src/components/OllamaStatus.tsx` | tsc --noEmit |
| 2026-04-15 | BUG-2 | `handleDrop` in InsuranceAnalysis ran guard twice (duplicate lines 63–65) | Copy-paste artifact during component creation | Removed duplicate `setIsDragging(false)` and `if (isExtracting\|\|isAnalyzing) return` | `src/components/InsuranceAnalysis.tsx` | tsc --noEmit |
| 2026-04-15 | BUG-3 | `JSON.parse` crashed with 500 when Ollama wrapped JSON in markdown fences | Ollama sometimes returns ````json ... ```` around structured output | Added `cleanJsonResponse()` helper + inner try/catch returning 422 | `api/src/index.ts` | tsc --noEmit |
| 2026-04-15 | BUG-4 | Ollama fetch requests hung indefinitely during cold-start | No `signal` passed to `fetch()` — no timeout on 7B model load | Added `AbortSignal.timeout(90_000)` to all Ollama fetch calls | `api/src/providers/ollama.ts` | tsc --noEmit |
| 2026-04-15 | SEC-1 | CORS wildcard — any origin could call the API | `app.use(cors())` with no options defaults to allow all origins | Replaced with custom CORS middleware using origin allowlist from `ALLOWED_ORIGINS` env var | `api/src/index.ts`, `.env.example` | tsc --noEmit |
| 2026-04-15 | SEC-2 | Prompt injection via `analysisJson` string concat | Client-supplied JSON was interpolated directly into LLM prompt | Wrapped data in `<DATA>...</DATA>` delimiters + updated system instructions | `api/src/index.ts` | tsc --noEmit |
| 2026-04-15 | SEC-3 | No request body validation on any API endpoint | No schema library used; any JSON accepted | Added Zod schemas (`ExtractBodySchema`, `RecommendTaxBodySchema`, `RecommendInsuranceBodySchema`) to all 4 endpoints | `api/src/index.ts`, `api/package.json` | tsc --noEmit |
| 2026-04-15 | SEC-4 | `multer` imported and in package.json but never used | Dead code from early prototype | Removed import + removed `multer`, `node-fetch`, `@types/node-fetch`, `@types/multer` from package.json | `api/src/index.ts`, `api/package.json` | tsc --noEmit |
| 2026-04-15 | DRY-3 | Functions double-exported in `src/lib/ai/index.ts` | Named exports at declaration + redundant re-export block at bottom | Removed the redundant `export { ... }` block at end of file | `src/lib/ai/index.ts` | tsc --noEmit |
| 2026-04-15 | LINT-1 | `(m: any)` in OllamaStatus — untyped Ollama `/api/tags` response | `res.json()` result was `any`, mapped with `(m: any) => m.name` | Cast response to `{ models?: Array<{ name: string }> }`, map with inferred types | `src/components/OllamaStatus.tsx` | eslint + tsc |
| 2026-04-15 | DRY-4 | `@` alias pointed to repo root (`.`) not `./src` | Initial Vite config used wrong base path for path alias | Fixed `vite.config.ts` + `tsconfig.json` paths to resolve to `./src` | `vite.config.ts`, `tsconfig.json` | tsc --noEmit |
| 2026-04-15 | DOCKER-1 | Deprecated `version: '3.8'` key in docker-compose.yml | Docker Compose v2 ignores the version key and warns | Removed `version` key entirely | `docker-compose.yml` | docker compose config |
| 2026-04-15 | DOCKER-2 | Ollama entrypoint used `sleep 5` before pulling models | Fixed sleep cannot guarantee ollama serve is ready | Replaced with `until curl -sf http://localhost:11434/` readiness loop | `docker-compose.yml` | manual review |
| 2026-04-15 | DOCKER-3 | `depends_on` used simple form — no health condition | Services could start before dependencies were ready | Added healthchecks to all 3 services; `depends_on` now uses `condition: service_healthy` | `docker-compose.yml` | docker compose config |
| 2026-04-15 | DRY-6 | All LLM prompts inlined in `api/src/index.ts` | Initial implementation put prompts inline for convenience | Extracted to `api/src/prompts.ts`; imported into `app.ts` | `api/src/prompts.ts`, `api/src/app.ts` | tsc --noEmit |
